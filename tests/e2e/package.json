{
  "name": "swimly-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end test suite for Swimly API and frontend",
  "scripts": {
    "test": "jest --verbose",
    "test:api": "jest --verbose --testPathPattern=api",
    "test:frontend": "jest --verbose --testPathPattern=frontend",
    "test:ci": "jest --ci --verbose"
  },
  "dependencies": {
    "@types/jest": "^29.5.0",
    "dotenv": "^17.3.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
