// src/utils/region.ts
// Convenience re-export of the regional config single source of truth.
// Import from here or from '../config/regions' directly; both resolve to the same module.

export * from '../config/regions';
